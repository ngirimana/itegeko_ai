# MinIO

The Docker Compose stack starts MinIO at:

- API: `http://localhost:9000`
- Console: `http://localhost:9001`

Default local credentials:

- User: `itegeko`
- Password: `itegeko123`

Create the `itegeko-documents` bucket before enabling private document uploads in production flows.
